<!DOCTYPE html>
<html xmlns:og="http://ogp.me/ns#" xmlns:fb="https://www.facebook.com/2008/fbml">
<head>
  <title><%= content_for?(:title) ? content_for(:title) : "Stanford+UCSD | #{t :PeerStudio}" %></title>
  <link href='//fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600|PT+Sans:700' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= analytics_init if Rails.env.production? %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta property="og:title" content="<%= content_for?(:title) ? content_for(:title) : "Stanford | talkabout" %>"/>
    <meta property="og:type" content="cause"/>
    <meta property="og:url" content="<%= "#{request.protocol}#{request.host_with_port}#{request.fullpath}" %>"/>
    <meta property="og:site_name" content="talkabout
    "/>
    <meta property="og:description" content="<%= (@project && !@project.description.blank?) ? @project.description.truncate(100) : "Collaborative human and machine grading." %>" />

    <link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro' rel='stylesheet' type='text/css'>
</head>
<body>
<div id="fb-root"></div>
<div id="wrapper">
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation" style="margin-bottom: 0"
   <% if current_user != true_user %>style="color: rgb(255, 255, 255); background-image: -webkit-linear-gradient(top, rgb(238, 95, 91), rgb(189, 54, 47)); background-color: rgb(189, 54, 47);"<%end%> >
        <div class="navbar-header">
          <% if @org %>
            <%= link_to @org.name, o_path(@org), :class => "navbar-brand", :style => "padding-left: 0;" %>
          <% else %>
            <%= link_to root_path, :class => "navbar-brand" do %>
              Stanford+UCSD | <strong><%= t :PeerStudio %></strong>
              <% if content_for?(:title) %>
                <small class="title_slug">&ndash; <%= content_for(:title) %></small>
              <% else %>
                <small class="title_slug">&ndash; Let's learn together!</small>
              <% end %>
            <% end %>
          <% end %>
        </div>
        <% if user_signed_in? %>
            <ul class="nav navbar-top-links navbar-right">
              <li class="dropdown">
                <a class="dropdown-toggle" id="login_label" role="button" data-toggle="dropdown" data-target="#" href="<%= edit_user_registration_path %>">
                  <%= current_user.name %>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu" role="menu" aria-labelledby="login_label">
                  <li><%= link_to "Account Settings", edit_user_registration_path %></li>
                  <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
                </ul>
              </li>
            </ul>
          <% else %>
            <ul class="nav navbar-top-links navbar-right">
              <li><%= link_to "Sign up / Sign in", new_user_session_path, :method => :get %></li>
            </ul>
          <% end %>
          <div class="navbar-default navbar-static-side" role="navigation">
                <div class="sidebar-collapse">
                  <%= content_for?(:context_menu) ? content_for(:context_menu) : render(:partial => 'layouts/global_context_menu') %>
                </div>
                <!-- /.sidebar-collapse -->
          </div>
  </nav>

<div id="page-wrapper">
<%= render :partial => 'layouts/notice', :locals => {:notice => notice, :alert => alert} %>
<%= yield %>

<div class="clearfix"></div>

<div style="border-top: 1px solid #efefef; margin-top: 15px;">
  <span class="pull-left" style="font-size:12pt"><i class="glyphicon glyphicon-question-sign
"></i> Got a question or need <%= link_to "help?", help_courses_path%></span>
  <a href="http://www.ucsd.edu/"><%= image_tag "ucsd.png", :style => 'height: 15%; width: 15%', :class => 'pull-right' %></a>
  <a href="http://cs.stanford.edu/"><%= image_tag "stanfordcs.png", :style => 'height: 15%; width: 15%; border-right: 1px solid #efefef; padding-right: 5px;', :class => 'pull-right' %></a>
  <br/>
  <% if Rails.env.development? || (user_signed_in? && current_user.admin?) %> 
    <p><span class="label">DEBUG</span> Parameters: <code> <%= params.inspect %> </code> Session: <code><%= session %></code> </p>
    <p><span class="label">OPENID</span> Return path <code><%= request.env['omniauth.origin']  || "None"%></code></p>
    <% if not flash[:debug].nil? %>
    <div class="well">
      <%flash[:debug].each do |msg| %>
      <%= msg %>
    <% end %>
    </div>
    <% end %>
    
  <% end %>
</div>

</div>
<script type="text/javascript">$('.dropdown-toggle').dropdown();</script>
</div>
</body>

</html>
